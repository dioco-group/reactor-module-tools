/**
 * Minimal language code type for the module preview tool.
 * In dioco-shared this is a more strictly validated type; for previewing
 * we only need it as a string.
 */
export type langCode_G_t = string;


